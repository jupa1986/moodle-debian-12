# Nombre de la carpeta de la instalacion y de la base de datos
app_name: moodle
moodle_git_branch: MOODLE_402_STABLE
# debian 11
php_version: 8.2
# debian 12
# php_version: 8.2
disable_cron: false
# Datos Base de datos
db_name: "{{ app_name }}"
db_user: "{{ app_name }}_user"
db_pass: cambiarPassword
# dominio para moodle
moodle_domain: edu.uif.gob.bo
# puerto para vagrant
ssh_port: 42324
max_uploadable_filesize: 200M
# usuario con privilegio de root sin necesidad de poner password
non_root_user: vagrant
